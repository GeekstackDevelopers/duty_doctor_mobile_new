enum Gender { male, female }

enum LocationType { onsite, hybrid, remote }

enum JobApplicationStatus {
  applied,
  rejected,
  shortlisted,
  selected,
  callScheduled,
  candidateAccepted,
  candidateRejected
}
